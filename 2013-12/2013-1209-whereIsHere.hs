module Dec9Where where 
-- factorial           :: (show (Double -> Double)
import PrImEs 

factorial'              = product [1..n]
                        where 
                                n = 11 

f  xs  			= sum xs`div` length xs 
--           where 
--           a    = 10 
--           xs   = [1,2,3,4,5]

-- average'  ns               = sum ns `div` length ns 
--                        where 
--                              ns  = factorial 
--                            n = 11 
  
